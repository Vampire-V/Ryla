# OS
.DS_Store
Thumbs.db

# Git worktrees
.worktrees/

# .NET
backend/**/bin/
backend/**/obj/
backend/**/*.user
backend/**/.vs/
backend/**/coverage/
backend/**/*.pfx
backend/**/*.lscache
backend/**/local.settings.json

# Frontend
frontend/.next/
frontend/out/
frontend/node_modules/
frontend/coverage/

# Supabase
supabase/.branches/
supabase/.temp/

# Google Cloud service account key files (NEVER commit)
ryla-*.json
client_secret_*.apps.googleusercontent.com.json

# Secrets — NEVER commit these
.env
.env.local
.env.*.local
.env.mcp
backend/src/**/appsettings.Development.json
backend/src/**/secrets.json

# dotnet user-secrets (ถูก store ใน ~/AppData หรือ ~/.microsoft แต่ป้องกันไว้)
backend/src/**/.secrets/

# Supabase local data (Docker volumes ไม่ต้องการใน git)
supabase/.branches/
supabase/.temp/
supabase/docker/volumes/

# Test results
coverage/
coverage-*/
TestResults/
*.trx
*.cobertura.xml

# IDEs
.idea/
.vscode/
*.suo

# Claude Code local settings (personal overrides, ไม่ share ระหว่าง developers)
.claude/settings.local.json

# Skills (reinstall via: npx skills install — lockfile: skills-lock.json)
.agents/skills/
.claude/skills/
skills/

# Other AI editor configs
.kiro/
.windsurf/

# AOT publish artifacts (local only)
aot-publish*/

# Demo screenshots (local only)
demo-*.png

# Local tooling
.playwright-mcp/
.superpowers/
.claude/review-passed.json
